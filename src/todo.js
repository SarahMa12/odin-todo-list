function createToDo(title, desc, dueDate, priority) {
    return {
        title,
        desc,
        dueDate,
        priority,
    };
}

function addToDoToProject(todo, projectFolder) {

}